import css from 'styled-jsx/css'
import { colors } from '../../../styles/theme'

export default css`
    .about-body > :global(p) {
        font-size: 1.2em;
        line-height: 1.75;
        color: ${colors.black}dd;
        margin-bottom: 0;
    }
`